(function tangentPolygonsAdapter(global) {
	"use strict";
	const core = global.SUPERMEGA_INSTRUMENT_ADAPTERS.core;
	const directions = ["flow", "spiral", "scatter"];
	const parentModes = ["previous", "any"];
	const presentations = ["crt", "reverse"];
	const radiusDistributions = ["uniform", "small", "alternating"];
	const tangencies = ["external", "internal"];
	const defaults = { direction: "flow", drawSpeed: 5, edgeCount: 3, edgeSpan: 4, guides: false, lineWidth: 1.4, offsetX: 18, offsetY: 11, parentMode: "previous", phase: 0.08, polygonMax: 8, polygonMin: 5, presentation: "crt", radiusDistribution: "uniform", radiusMax: 72, radiusMin: 34, repeatCount: 4, seed: 1976, steps: 24, tangency: "external", turnBias: 0.18 };
	const fields = {
		direction: { type: "enum", label: "Direction", group: "chain", choices: directions }, drawSpeed: { type: "number", label: "Draw speed", group: "playback", minimum: 1, maximum: 12, step: 0.5 }, edgeCount: { type: "integer", label: "Visible edges", group: "polygon", minimum: 1, maximum: 12, step: 1 }, edgeSpan: { type: "integer", label: "Edge span", group: "polygon", minimum: 1, maximum: 12, step: 1 }, guides: { type: "boolean", label: "Construction guides", group: "look" }, lineWidth: { type: "number", label: "Line width", group: "look", minimum: 0.5, maximum: 6, step: 0.1 }, offsetX: { type: "integer", label: "Repeat X", group: "repeat", minimum: -120, maximum: 120, step: 1 }, offsetY: { type: "integer", label: "Repeat Y", group: "repeat", minimum: -120, maximum: 120, step: 1 }, parentMode: { type: "enum", label: "Parent", group: "chain", choices: parentModes }, phase: { type: "number", label: "Polygon phase", group: "polygon", minimum: 0, maximum: 1, step: 0.005 }, polygonMax: { type: "integer", label: "Maximum sides", group: "polygon", minimum: 3, maximum: 16, step: 1 }, polygonMin: { type: "integer", label: "Minimum sides", group: "polygon", minimum: 3, maximum: 16, step: 1 }, presentation: { type: "enum", label: "Presentation", group: "look", choices: presentations }, radiusDistribution: { type: "enum", label: "Radius distribution", group: "chain", choices: radiusDistributions }, radiusMax: { type: "number", label: "Maximum radius", group: "chain", minimum: 12, maximum: 180, step: 1 }, radiusMin: { type: "number", label: "Minimum radius", group: "chain", minimum: 12, maximum: 180, step: 1 }, repeatCount: { type: "integer", label: "Translated copies", group: "repeat", minimum: 1, maximum: 12, step: 1 }, seed: { type: "integer", label: "Seed", group: "chain", minimum: 0, maximum: 4294967295, step: 1 }, steps: { type: "integer", label: "Circles", group: "chain", minimum: 2, maximum: 96, step: 1 }, tangency: { type: "enum", label: "Tangency", group: "chain", choices: tangencies }, turnBias: { type: "number", label: "Turn bias", group: "chain", minimum: -1, maximum: 1, step: 0.01 },
	};
	// null and "" must NOT read as 0. The adapter core JSON-clones every patch,
	// so NaN and Infinity arrive as null; coercing that to 0 would silently slam a
	// control to its floor instead of ignoring bad input.
	const usableNumber = (value) => value !== null && value !== undefined && value !== "";

	function number(value, fallback, min, max) { const parsed = usableNumber(value) ? Number(value) : Number.NaN; return Math.min(max, Math.max(min, Number.isFinite(parsed) ? parsed : fallback)); }
	function choice(value, choices, fallback) { return choices.includes(value) ? value : fallback; }
	function normalize(input) {
		input = input && typeof input === "object" && !Array.isArray(input) ? input : {};
		const rawRadiusMin = number(input.radiusMin, defaults.radiusMin, 12, 180); const rawRadiusMax = number(input.radiusMax, defaults.radiusMax, 12, 180); const radiusMin = Math.min(rawRadiusMin, rawRadiusMax); const radiusMax = Math.max(rawRadiusMin, rawRadiusMax);
		const rawPolygonMin = Math.round(number(input.polygonMin, defaults.polygonMin, 3, 16)); const rawPolygonMax = Math.round(number(input.polygonMax, defaults.polygonMax, 3, 16)); const polygonMin = Math.min(rawPolygonMin, rawPolygonMax); const polygonMax = Math.max(rawPolygonMin, rawPolygonMax);
		const edgeSpan = Math.round(number(input.edgeSpan, defaults.edgeSpan, 1, polygonMin)); const edgeCount = Math.round(number(input.edgeCount, defaults.edgeCount, 1, edgeSpan));
		return { direction: choice(input.direction, directions, defaults.direction), drawSpeed: number(input.drawSpeed, defaults.drawSpeed, 1, 12), edgeCount, edgeSpan, guides: input.guides === undefined ? defaults.guides : Boolean(input.guides), lineWidth: number(input.lineWidth, defaults.lineWidth, 0.5, 6), offsetX: Math.round(number(input.offsetX, defaults.offsetX, -120, 120)), offsetY: Math.round(number(input.offsetY, defaults.offsetY, -120, 120)), parentMode: choice(input.parentMode, parentModes, defaults.parentMode), phase: number(input.phase, defaults.phase, 0, 1), polygonMax, polygonMin, presentation: choice(input.presentation, presentations, defaults.presentation), radiusDistribution: choice(input.radiusDistribution, radiusDistributions, defaults.radiusDistribution), radiusMax, radiusMin, repeatCount: Math.round(number(input.repeatCount, defaults.repeatCount, 1, 12)), seed: Math.round(number(input.seed, defaults.seed, 0, 4294967295)), steps: Math.round(number(input.steps, defaults.steps, 2, 96)), tangency: choice(input.tangency, tangencies, defaults.tangency), turnBias: number(input.turnBias, defaults.turnBias, -1, 1) };
	}
	const schema = { id: "supermega.instrument.configuration/cga76-tangent-polygons/v1", version: 1, fields, defaults };
	const definition = { actions: { "create-artifact": ({ options }) => options.createArtifact(), "hide-controls": ({ options }) => options.hideControls(), pause: ({ options }) => options.pause(), randomize: ({ apply, configuration, options }) => apply({ ...configuration, ...options.nextSeed() }, "randomize"), resume: ({ options }) => options.resume(), "show-controls": ({ options }) => options.showControls() }, capabilities: ["configure", "copy-config", "reset-configuration", "randomize", "pause", "resume", "show-controls", "hide-controls", "create-artifact"], defaults, id: "cga76-tangent-polygons", normalize, schema };
	global.SUPERMEGA_INSTRUMENT_ADAPTERS.cga76TangentPolygons = Object.freeze({ create(options) { const required = ["applyConfiguration", "createArtifact", "hideControls", "nextSeed", "pause", "resume", "showControls"]; if (!options || required.some((name) => typeof options[name] !== "function")) throw new TypeError(`Tangent Polygons adapter requires page callbacks: ${required.join(", ")}.`); return core.createAdapter(definition, options); }, defaults: Object.freeze(normalize(defaults)), schema: Object.freeze(schema) });
})(window);
