Build a standalone SUPERMEGA Instrument called “Text Image Matrix,” credited to Kenneth Knowlton's Universal Declaration of Human Rights (1971), reproduced in Computer Graphics and Art, May 1976 p. 13.

Reconstruct the documented mechanism without copying the scan, portrait, later translation code, or full declaration: repeat a bounded editable corpus into deterministic fixed-column rows, sample an original procedural or local source image per glyph, and modulate readable micro-text so distance reveals a macro image. Draw a self-contained five-by-seven bitmap alphabet with deterministic unsupported-character marks so replay never depends on platform font loading. Use a classic-script, Node-testable core and Canvas 2D only. No cross-tool renderer import.

Treat local input as a security and privacy boundary. Accept static PNG/JPEG/WebP only. Check 20 MB before reading; validate magic, structure, dimensions, animation markers, PNG CRC, MIME agreement, and a 20-megapixel cap before decode allocation. Use asynchronous generation tokens. Preserve the last valid image on every rejection. Revoke temporary object URLs and close decoded bitmaps on replacement, mismatch, failure, stale completion, clear, and unload. Reset must retain the upload; clear-renderer removes it. Never serialize or expose image bytes, filename, path, URL, or local-source hashes.

The named adapter owns JSON-safe configuration and declares configure, copy-config, reset-configuration, clear-renderer, show-controls, hide-controls, and create-artifact—no motion or random action. Use a strict same-origin bridge whose per-request authorization blocks image-derived create-artifact until the operator grants one in-frame export. Direct in-frame export remains allowed.

Ship index.html, text-image-core.js, text-image-page.js, test.mjs, shot.mjs, NOTES.md, this prompt, a 1600×1000 JPEG preview, named adapter, and adapter tests. Pin Instrument release 0.1.0-1ff60f2032a6. Include collection breadcrumb, mobile dock-hidden layout, visible three-row documented/interpreted/Supermega provenance, fixed HTTPS sources, 16 KB versioned URL/load behavior, safe text sinks, DPR live Canvas, and separate 1600×1000 PNG artifact Canvas. Prove exact same-environment RGBA replay plus the complete privacy/resource lifecycle.
