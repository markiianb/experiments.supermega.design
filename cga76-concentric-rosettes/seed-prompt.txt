Build a standalone SUPERMEGA Instrument called “Concentric Segment Rosettes,” credited jointly to Charles J. Fritchie and Robert H. Morriss and figure eight from their article Inexpensive Graphics from a Storage Cathode Ray Tube, reproduced in Computer Graphics and Art, May 1976 pp. 5 and 32.

Treat this as a separate program from Tangent Polygon Trails. Give it a separate page-owned renderer core. Do not import, call, or expose tangent-chain logic. The source default has exactly one center. Generate a configured radius sequence, inscribe a regular polygon on every ring, and emit only the direct consecutive edge subset selected by segment start and span. Every ring within a rosette must share one center and every vertex must lie exactly on its assigned radius.

Expose ring count, inner/outer radius, linear/geometric/eased spacing, polygon side range, phase, phase drift, segment start/span, deterministic dropout, line width, CRT/reverse presentation, draw speed, and a clearly labeled multi-center extension. The paper did not print those constants or rules, so label them as interpretation. Use a fixed index function for dropout. Do not expose a seed or randomize action, and do not call Math.random.

The named adapter owns all configuration and declares configure, copy-config, reset-configuration, pause, resume, show-controls, hide-controls, and create-artifact only. Configure/reset returns finite drawing playback to phase zero; pause freezes; export always paints the canonical final frame on a clean 1600×1000 Canvas. Reduced motion opens final and stays paused until explicit Resume.

Build a page-owned classic-script core and controller, 16 KB versioned URL/load state, strict same-origin bridge, safe text sinks, chrome-safe DPR live Canvas, 42/58 mobile controls, collection breadcrumb, full provenance, source and visibly distinct Supermega presets, and fixed PNG export. Ship index.html, rosette-core.js, rosette-page.js, test.mjs, shot.mjs, NOTES.md, this prompt, an optimized 1600×1000 JPEG preview, named adapter, and adapter tests. Pin Instrument release 0.1.0-1ff60f2032a6. Prove shared centers, exact radius sequence, vertex radii, direct edge subsets, deterministic phase/dropout, one-center source default, absence of seed/randomize/tangent state, motion freeze/resume, hostile input safety, mobile layout, fixed artifact metadata, and portable file:// behavior.
