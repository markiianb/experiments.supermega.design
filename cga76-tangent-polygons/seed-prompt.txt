Build a standalone SUPERMEGA Instrument called “Tangent Polygon Trails,” credited jointly to Charles J. Fritchie and Robert H. Morriss and their article Inexpensive Graphics from a Storage Cathode Ray Tube, reproduced in Computer Graphics and Art, May 1976 pp. 5 and 32.

Reimplement the written geometry without copying or tracing a printed figure. Construct a seeded sequence of tangent circles. External mode places each new center at the sum of parent and child radii; internal mode uses the declared absolute radius difference. Inscribe a regular polygon on every circle and emit an exact unique subset of its edges. Repeat the base trail by exact configured translation vectors. Keep CRT light-on-dark and reverse-photograph dark-on-light presentation separate from construction so inversion, line width, and guides cannot alter geometry.

Name every reconstruction choice: seed, chain length, radius range and distribution, direction, turn bias, external/internal tangency, immediately-previous or any-earlier parent, polygon side range, visible edge count and span, phase, repeat count, X/Y offsets, line width, construction guides, draw speed, and presentation. Label any-earlier parent and all constants as interpretations. Credit both authors even where a later index credits Fritchie alone.

Use a local audited PRNG and no Math.random. The named adapter owns all configuration and declares configure, copy-config, reset-configuration, randomize, pause, resume, show-controls, hide-controls, and create-artifact. Randomize changes seed only. Configure/reset returns finite drawing playback to phase zero; pause freezes; export always paints the canonical final frame on a clean 1600×1000 Canvas. Reduced motion opens final and stays paused until explicit Resume.

Build a page-owned classic-script core and controller, 16 KB versioned URL/load state, strict same-origin bridge, safe text sinks, chrome-safe DPR live Canvas, 42/58 mobile controls, collection breadcrumb, full provenance, source and visibly distinct Supermega presets, and fixed PNG export. Ship index.html, tangent-core.js, tangent-page.js, test.mjs, shot.mjs, NOTES.md, this prompt, an optimized 1600×1000 JPEG preview, named adapter, and adapter tests. Pin Instrument release 0.1.0-1ff60f2032a6. Prove tangent distances, vertex radii, edge subset/count, repeat vectors, deterministic replay, geometry-preserving inversion, motion freeze/resume, hostile input safety, mobile layout, fixed artifact metadata, and portable file:// behavior.
