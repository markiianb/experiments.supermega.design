Build a dependency-free classic-script SUPERMEGA Instrument called Sykora Element Matrix, credited to Zdeněk Sýkora working with Jaroslav Blažek, based on Structure and Black-White Structure in Computer Graphics and Art, May 1976 p. 17. Pin Instrument 0.1.0-1ff60f2032a6 and support file:// plus HTTP.

Create a new coded library of square modules made from semicircles. Each module has four color-edge bits, four shape/half-circle connection bits, local arc geometry, and an intensity. Implement four named Boolean rules representing the four color-continuation/shape-continuation combinations. Scan stable row-major order by a canonical four-rule priority list. Keep user initial assignments and +/- markers fixed. Strict mode uses bounded deterministic backtracking; permissive mode leaves unresolved cells. Do not introduce chance or call Math.random anywhere in the core.

The named adapter solely owns grid size, repertoire, bounded initial cells, four rule priorities, color/shape weights, coefficient, direction, two palettes, grid angle, solve mode, and build speed. Capabilities are configure/copy/reset/show/hide/create plus pause/resume. There is no randomize. Canvas click/touch and keyboard editing must update initial cells through the adapter.

Use exact version/type URL envelopes, visible bare-JSON loading, a 16 KB bound, literal text/value sinks, DPR canvas, final 1600x1000 offscreen export, strict bridge, inert hidden controls, pressed preset state, and a mobile stage above a bottom sheet. Reduced motion shows the final scan and rejects Resume/Replay. Show compact exact evidence labels Documented mechanism / Interpretation / Supermega, pending-review disclosure, source and distinct two-grey Supermega presets, HTTPS sources, core and adapter tests, notes, shot, and optimized preview. Ship no scans, photos, or later code.
